# ada-project

## Dataset

The dataset is not pushed on the github repository. Place it in the folder name `data/` at the root of the project.

## IPython Workflow 

- Do `Kernel > Restart & Clear Output` and save before commit

